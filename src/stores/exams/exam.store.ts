import { createSlice } from '@reduxjs/toolkit';

const initialState = {};
const examSlice = createSlice({
    name: 'exam',
    initialState,
    reducers: {},
});

//   export const { setUserItem } = examSlice.actions;

export default examSlice.reducer;
